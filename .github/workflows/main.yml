name: aprendendo git actions
on:
  push:
  workflow_dispatch: 

  jobs:
    test:
      runs-on: ubuntu-latest
      enviroment: env
      steps:
        - name: Print secret
          run: echo "A senha é ${{ secrets.SECRET }}"branch_protection_rule: 

          - name: Print variables
            run: echo "A senha é ${{ vars.VARIABLE }}"
